/** @type {import('tailwindcss').Config} */
module.exports = {
    theme: {
      extend: {
        colors: {
          cream: "var(--cream)", // Gunakan warna dari CSS Variables
        },
      },
    },
    plugins: [],
  };